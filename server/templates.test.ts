import { describe, expect, it } from "vitest"; import { normalizeTemplate, templateSnapshot } from "./templates.ts";
describe("task templates", () => { it("validates and hides instructions from catalogs", () => { const item = normalizeTemplate({ id: "research", name: "Research", instructions: "Find sources" }); expect(templateSnapshot(item)).not.toHaveProperty("instructions"); }); it("rejects incomplete input", () => expect(() => normalizeTemplate({ name: "x" })).toThrow(/needs a name/)); });
